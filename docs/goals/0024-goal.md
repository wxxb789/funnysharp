/goal
Independently determine whether the next-stage FunnySharp candidate satisfies every accepted completion contract in Goals 14–23, and issue a fail-closed evidence-backed verdict without modifying implementation to conceal findings. The audit must evaluate the actual packages and public APIs, not merely repository prose or a green workflow, and must cover the product constitution and complete pragmatic-capability decisions; the canonical Option, Result, UnitResult, and Validation semantics; functional grammar and consumer expression density; BCL collection rigor and contextual traversal; async-first streaming and structured concurrency; curated effects, state, resources, and opt-in immutable patterns; performance trust; compiler-guided AI usability and analyzers; the package-consuming ASP.NET Core proof; and release documentation, packaging, trimming, Native AOT, and compatibility claims. It must also verify the exclusions: no competitor dependency or compatibility layer, duplicate Maybe carrier, custom execution runtime or scheduler, replacement collection universe, pervasive immutable-data tax, HKT or monad-transformer hierarchy, hidden sync-over-async, and premature general discriminated-union system. Completion evidence is a goal-by-goal pass, fail, or unverified matrix linking every judgment to reproducible tests, package-consumer runs, API and dependency inventories, analyzer results, AI-evaluation records, benchmarks with equivalence and environment evidence, and precise minimal reproductions for every material gap. Claims that cannot be independently reproduced remain unverified. Any material failed or unverified criterion forces an overall non-pass verdict. This goal is an audit boundary only: it may report defects and missing evidence but must not implement fixes, redefine prior goals, weaken thresholds, or broaden scope during verification.
